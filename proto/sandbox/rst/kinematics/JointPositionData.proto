package rst;

option java_outer_classname = "JointPositionDataContainer";

import "rst/kinematics/JointData.proto";

// A message for joint position data of a robot seen from its head.
message JointPositionData {
    repeated JointData joints = 1;
}
