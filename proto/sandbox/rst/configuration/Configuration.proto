package rst.configuration;

import "rst/configuration/Option.proto";

option java_outer_classname = "ConfigurationType";

/**
 * A configuration consisting of @ref Option entries.
 *
 * @author Jan Moringen <jmoringe@techfak.uni-bielefeld.de>
 */
message Configuration {

    /**
     * Collection of configuration options.
     *
     * Duplicate values of @ref Option.name are not permitted.
     *
     * Order of entries is not significant.
     */
    repeated Option option = 1;

};
