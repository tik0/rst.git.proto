/**
 * Tracking refers to the process of maintaining an estimate of some
 * property, such as the position, of a tracked subject over time.
 *
 * This package contains data types for representing tracked subjects
 * (@ref .TrackingInfo), changes in tracking quality (@ref .State) and
 * tracked properties (@ref .TrackedPosture3DFloat, @ref
 * .TrackedPosition3DFloat).
 */
package rst.tracking;
